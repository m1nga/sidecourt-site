Canonical SideCourt application and independent /admin/. Addresses are real paths: each fixed page has its own folder holding the application, and 404.html answers /work/<id> and /people/<handle>.
Former preview and CleanPause addresses still redirect. Older /#court style addresses are translated by the application itself on arrival.
Merge with existing product downloads and updates, retaining Google ownership metadata; never replace the entire hosting output with this overlay.
This export does not certify SMTP, AI, backup recovery or complete launch acceptance.
